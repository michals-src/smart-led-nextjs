import { createContext } from 'react';

export const BottomsheetContext = createContext<any>(null);
export const BottomsheetThemeContext = createContext<any>(null);
export const BottomsheetItemContext = createContext<any>(null);